# dsh 全局指令（agent-instructions 自动注入，每个新对话第一轮生效）

## 第一件事：读取环境配置
开始处理任何任务之前，必须先完整读取 /workspace/ENV.md
（其中包含红线政策、网络代理、远程 VM、目录结构等环境信息）。
ENV.md 内容未确认在上下文中时，一律以重新读取文件为准，禁止凭记忆行事。

## 红线（来自 ENV.md，最高优先级，任何任务都适用）
1. NAS(192.168.36.5) 是生产环境，不是沙箱：对 NAS 的任何改动
   ——SSH 操作、docker 命令、配置修改、文件变更——必须先向用户说明并取得二次确认。
2. 本容器只能动 /workspace，禁止改动容器系统路径（/data/dsh 的 profile/patch/settings 等
   属于部署配置，未经用户明确要求不得修改）。
3. 外网受限必须走代理（Clash @192.168.36.33，先 source /workspace/proxy.conf）；
   禁止设置全局默认代理，否则会劫持 GUI。
4. 远程 VM(ubuntu-vm @192.168.36.18) 是隔离环境，可以放心操作，但仍需符合任务要求。
   默认实验/验证环境就是它（坏了不影响主功能）；旧 VM .15 系统老资源少，不推荐使用，
   除非用户明确要求不要碰它。

## 通用准则
- 用户没让你做的事不要自作主张；不确定先问。
- 宁可多问一次，不可擅自操作（尤其涉及 NAS / 容器系统路径 / 全局配置）。
- ENV.md 内容与本文件冲突时，以 ENV.md 的最新内容为准。
- **产出/长内容优先写文件，聊天里只给摘要和链接**（用户明确要求省 token）：
  把完整内容写入 /workspace 下的文件，然后在回复里给出**绝对外网链接**
  `https://harness.ericality.com/files/<路径>`（链接格式见 ENV.md），
  不要在聊天里粘贴大段全文。链接必须可点（绝对 http(s) URL，相对链接不渲染）。
