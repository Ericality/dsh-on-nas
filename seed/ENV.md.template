# ENV.md 模板 (seed 提供) —— 首次释放后, 请把本文件里的 <你的...> 占位替换为真实值

ENV (agent read first) | 2026-08-21 | wd=/workspace

== 红线(最高优先级) ==
NAS(192.168.36.5)=生产环境! 对其任何改动(SSH/docker/配置/文件)必须先向用户说明并取得二次确认。
只有VM .18可随便折腾。本容器=NAS上的dsh-on-nas, 只能动/workspace, 禁止动容器系统路径
(曾多位agent乱改导致崩溃循环)。实验性操作优先放VM .18, 不在本容器做。

== 网络 ==
外网受限必须走代理(Clash @192.168.36.33, VM内同样可用):
  http=7890 socks=7891 mixed=7893 redir=7892 tproxy=7895(后两者需iptables,普通客户端不可用)
用法: source /workspace/proxy.conf → proxy-on/off/status
坑: socks必须用--socks5-hostname(远程DNS),直接--socks5挂起; 勿设全局代理(劫持GUI 3080)

== 远程VM .18 (隔离环境,放心搞) ==
ssh ubuntu-vm (shahao@192.168.36.18, 密钥/data/dsh/.ssh/id_ed25519, 免密)
Ubuntu 26.04 / 4核 / 3.3G / sudo免密
已装: py3.14.4+pip25, uv0.12.5, node22.22.1+npm9.2, docker29.1.3(免sudo,daemon代理已配)
npm/pip代理持久化; apt国内镜像直连; 已恢复旧VM数据(Warp2Clash/.bash_history/.gnupg)
注意:~在ssh config解析为/root,用绝对路径; 装系统时cloud-init卡→强制重启解决

== 旧VM .15 (别名 ubuntu-vm-legacy) ==
仍在线但系统老资源少, 仅存档, 默认不用, 除非用户明确要求。

== 海外服务器 (别名 ssh overseas) ==
cloudclone.ericality.com @203.88.116.223 / Ubuntu 24.04 / 2核 / 961M / 59G
py3.12.3 + docker29.3.0已装, 无node, 密钥+sudo免密已配
海外无网络限制(不用代理) 但链路不稳定(已配ServerAliveInterval 30)
⚠️ 局域网代理对其不可达(不同内网); ⚠️ 启用需用户单独确认!

== 目录 ==
/workspace/projects/: bing-wallpaper/(已跑通,.venv) telegram_media_downloader_enhanced/(需代理)

== 文件链接格式 ==
外网: https://harness.ericality.com/files/<路径> | 局域网: http://192.168.36.5:3080/files/<路径>
必须绝对URL(相对不渲染); 两个入口登录会话分开; fileserve限/workspace只读

== 本机 ==
root / 无沙箱 / py3.11.2(pip23,venv) / 无docker

== 警告 ==
部署容器非沙箱! 禁改容器系统路径(/usr/local/bin /etc /opt), 即使root——搞坏崩溃循环
(报错dsh-entrypoint permission denied), 需NAS侧 docker compose up -d --force-recreate 重建。

== GitHub(dsh-on-nas) ==
repo: Ericality/dsh-on-nas  本地: projects/dsh-nas/(b77f0b7)
PAT: <GITHUB_PAT> (workflows读写+Secrets读写, 2026-08-23已授权)
镜像: ghcr.io/ericality/dsh-nas(-https), 用户名小写!
访问: 外网CF Tunnel回源http://NAS:3080; 局域网https://NAS:8443(自签)
坑: 登录掉线→SameSite改Lax已修; 实时更新走WS(/api/events)非SSE,需WS升级转发(已配,101)

== Docker Hub (ericality, 2026-08-23) ==
token: <DOCKER_PAT> (落盘: /workspace/.credentials.docker, chmod 600)
用法: 仓库 Actions secrets 已配 DOCKERHUB_USERNAME=ericality + DOCKERHUB_TOKEN(同上)
teslaMateAddressFixer 的 workflow 双推: ghcr.io/ericality/teslamate-fixer + ericality/teslamate-fixer (amd64+arm64)
⚠️ GitHub 坑: if: 条件里直接引用未定义 secrets.* 会让 workflow 校验失败(0 jobs);
   正确做法: job 级 env 映射 secret 后用 if: env.X != '' 判断
NAS 拉取: docker.1ms.run 是 Docker Hub 镜像, NAS 继续用 docker.1ms.run/ericality/teslamate-fixer:latest 即可
验证: hub.docker.com/v2/user/ 用 token 登录; 或 POST /v2/users/login/ {username,password:token}

== NAS .5 (生产, 改动需二次确认) ==
Synology DS1821+ / Docker 20.10.23 / user=shahao(密码未落盘)
本容器(112.x)无法直连NAS(36.x): ssh -J ubuntu-vm shahao@192.168.36.5 (跳板已实测)
备选: HTTP CONNECT 经192.168.36.33:7890
✅ 新发现(2026-08-23实测): 本容器(dsh)的 docker 网桥网关 172.25.0.1 = NAS 宿主,
   ssh shahao@172.25.0.1 可直连(22端口开放, 密码认证), 免跳板!
   用法: python3 /workspace/nas_ssh.py '远程命令' (SSH_ASKPASS自动喂密码,
   输出自动打码; 密码即 shahao 登录密码, 见 .credentials 备注/询问用户)
   docker 需 sudo: 命令里用 echo '密码' | sudo -S -p '' /usr/local/bin/docker ...
   ⚠️ sudo 凭据不跨 ssh 会话, 每条命令都要带密码
docker: /usr/local/bin/docker(PATH无), sudo -S密码, socket属root:root
重启本容器=NAS侧 docker compose up -d --force-recreate; 镜像ghcr.io/ericality/dsh-nas:latest
坑: NAS直连ghcr慢/dockerhub超时→crane走代理pull+ssh传+docker load
✅ 2026-08-23实测镜像传输路径: NAS直连hub/mirror超时挂起时 →
   VM: docker pull --platform linux/amd64 <img> (VM daemon带代理, 快)
   VM→NAS: docker save | gzip | SSH_ASKPASS(密码) setsid ssh shahao@192.168.36.5 "cat > /tmp/x.tar.gz"
   NAS: sudo docker load -i /tmp/x.tar.gz && sudo docker tag <img> 需要的名字
   注意: VM 上 ssh 用 askpass 必须 export DISPLAY=:0, 否则不生效
